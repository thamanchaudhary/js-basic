// User proper variable names and camelCase

const firstName = "Thaman";
const lastName = "Tharu";

const videos = assets.filter((asset) => asset.type === "video");
/*
    1. Use proper variable names and camelCase
    2. Use arrow function syntax
    3. Use filter method to filter out the videos
*/

// Be careful with comparison using the loose equality operator
[100] == 100; // true
// [100] === 100; // false  //

"100" == 100; // true
"100" === 100; // false
